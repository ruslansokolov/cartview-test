import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    minWidth: 300,
    minHeight: 100,
    borderRadius: 5,
    borderWidth: 1,
    borderColor: '#000000',
  },
  summary: {
    padding: 10,
  },
  spinner: {
    position: 'absolute',
    width: '100%',
    height: '100%',
    backgroundColor: '#ffffffcc'
  },
})